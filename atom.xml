<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zwangsheng.github.io</id>
    <title>-wangSheng-</title>
    <updated>2021-01-08T03:02:30.730Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zwangsheng.github.io"/>
    <link rel="self" href="https://zwangsheng.github.io/atom.xml"/>
    <subtitle>-wangsheng-</subtitle>
    <logo>https://zwangsheng.github.io/images/avatar.png</logo>
    <icon>https://zwangsheng.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, -wangSheng-</rights>
    <entry>
        <title type="html"><![CDATA[ZooKeeper]]></title>
        <id>https://zwangsheng.github.io/post/zookeeper/</id>
        <link href="https://zwangsheng.github.io/post/zookeeper/">
        </link>
        <updated>2021-01-07T08:18:18.000Z</updated>
        <content type="html"><![CDATA[<h1 id="节点">节点</h1>
<h2 id="persistent-持久节点">PERSISTENT-持久节点</h2>
<p>除非手动删除，不然一直存在ZooKeeper上</p>
<h2 id="ephemeral-临时节点">EPHEMERAL-临时节点</h2>
<p>临时节点的生命周期与客户端的会话绑定，客户端会话失效，那么ZooKeeper的节点就会失效</p>
<h2 id="persistent_sequential-持久顺序节点">PERSISTENT_SEQUENTIAL-持久顺序节点</h2>
<p>增加了顺序</p>
<h2 id="ephemeral_sequential-临时顺序节点">EPHEMERAL_SEQUENTIAL-临时顺序节点</h2>
<p>增加了顺序</p>
<h1 id="watcher进行事件通知">Watcher进行事件通知</h1>
<p>一次性</p>
<h2 id="客户端注册watcher">客户端注册Watcher</h2>
<h2 id="服务端处理watcher">服务端处理Watcher</h2>
<h2 id="客户端回调watcher">客户端回调Watcher</h2>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[RocketMQ]]></title>
        <id>https://zwangsheng.github.io/post/rocketmq/</id>
        <link href="https://zwangsheng.github.io/post/rocketmq/">
        </link>
        <updated>2021-01-07T08:18:00.000Z</updated>
        <content type="html"><![CDATA[<h1 id="为啥选择mq">为啥选择MQ</h1>
<h2 id="异步">异步</h2>
<p>异步调用提高响应速度</p>
<h2 id="解耦">解耦</h2>
<p>降低系统耦合</p>
<h2 id="削峰">削峰</h2>
<p>请求到达峰值之后，保证后端固定速度消费</p>
<h1 id="rocketmq的架构">RocketMQ的架构</h1>
<h2 id="namesever">NameSever</h2>
<p>无状态列表</p>
<h2 id="borker">Borker</h2>
<p>负责发送消息，持久化消息</p>
<h2 id="producer">Producer</h2>
<p>消息的生产者，发送消息到Broker</p>
<h2 id="consumer">Consumer</h2>
<p>消息的消费者，从Broker接受消息消费</p>
<h2 id="queue">queue</h2>
<p>Topic是抽象的概念，实际的底层是多个queue组成，数据是真实的存在queue上的</p>
<h1 id="borker的消息消费后会立刻删除吗">Borker的消息消费后，会立刻删除吗</h1>
<p>每条消息都会持久化到commoit Log上，consumer消费之后只是更改了commit log的偏移量</p>
<h2 id="那么消费过的消息是否会造成消息堆积">那么消费过的消息是否会造成消息堆积</h2>
<p>默认48个小时之后会删除不再使用的commit log检查最后使用时间，指定时间删除</p>
<h1 id="消费方式">消费方式</h1>
<h2 id="集群消费">集群消费</h2>
<p>一条消息只会被同一个Group的一个consumer消费</p>
<p>如果存在不同的Group，那么每个Group都会有一个consumer消费这个消息</p>
<h2 id="广播消费">广播消费</h2>
<p>消息对于同一个Group的consumer都会消费一遍</p>
<h1 id="消费消息是pull还是push">消费消息是pull还是push</h1>
<p>RocketMQ没有真正意义上的push，都是pull</p>
<p>虽然有push类，但实际采用了长轮训机制，还是pull</p>
<h2 id="为啥采用pull不用push">为啥采用pull不用push</h2>
<p>如果broker推送消息，consumer的消费能力不确定，那么会造成消息堆积不能被其他consumer消费的问题</p>
<h1 id="重复消费">重复消费</h1>
<h2 id="原因">原因</h2>
<p>网络的不稳定性</p>
<h3 id="ack">ACK</h3>
<p>consumer完成消费之后应该返回ACK，Broker会将消息从queue上移除</p>
<p>当ack因为网络原因无法发送到borker的时候，broker会认为这个消息没有被消费，就会进行重投</p>
<h2 id="解决方式">解决方式</h2>
<ol>
<li>数据库</li>
<li>redis</li>
</ol>
<h1 id="顺序消费">顺序消费</h1>
<h2 id="生产端">生产端</h2>
<p>投送到同一个queue，MessageQueueSelector接口，确定topic下投送的queue</p>
<h2 id="消费端">消费端</h2>
<p>一个consumer进行单线程消费</p>
<h1 id="消息不丢失">消息不丢失</h1>
<h2 id="producer-2">Producer</h2>
<p>同步发送消息，发送失败之后进行重试</p>
<h2 id="borker-2">Borker</h2>
<p>同步刷盘，同步持久化到磁盘</p>
<h2 id="consumer-2">Consumer</h2>
<p>完全消费正常后手动ACK</p>
<h1 id="消息堆积">消息堆积</h1>
<p>定位问题所在</p>
<h2 id="conusmer-和-queue数量">Conusmer 和 queue数量</h2>
<p>准备一个临时的topic，其中queue的数量是堆积的几倍，上线一台consumer进行分发旧消息，上线多台consumer处理临时的topic<br>
注意queue的数量要大于consumer，不然有的consumer就闲着</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Redis]]></title>
        <id>https://zwangsheng.github.io/post/redis/</id>
        <link href="https://zwangsheng.github.io/post/redis/">
        </link>
        <updated>2021-01-07T08:17:34.000Z</updated>
        <content type="html"><![CDATA[<h1 id="数据结构">数据结构</h1>
<h2 id="string-key-value">String key-value</h2>
<p>字符串</p>
<h2 id="map-key-field-value">Map key field value</h2>
<p>哈希表</p>
<h2 id="list">List</h2>
<p>链表<br>
可以进行左边插入左边弹出，右边插入右边弹出</p>
<h2 id="set">Set</h2>
<p>不重复集合</p>
<h2 id="zset">ZSet</h2>
<p>有序不重复集合</p>
<h1 id="缓存问题">缓存问题</h1>
<h2 id="缓存穿透">缓存穿透</h2>
<p>查询一个不存在的数据，缓存中没有，数据库当中也没有</p>
<h3 id="解决">解决</h3>
<ol>
<li>存储空对象<br>
对于热点key值缓存个空值，不去数据库，在缓存直接返回</li>
<li>布隆过滤器<br>
缓存的基础上加上布隆过滤器<br>
先在缓存查找，找不到进入布隆过滤器，查到了有key，进入DB，并且更新缓存</li>
</ol>
<h2 id="缓存击穿">缓存击穿</h2>
<p>key值刚好过期，有一堆刚好的请求过来</p>
<h3 id="解决方法">解决方法</h3>
<ol>
<li>分布式锁 让一个请求去查，然后更新缓存</li>
<li>手动续期</li>
</ol>
<h2 id="缓存雪崩">缓存雪崩</h2>
<p>无法提供缓存服务，导致所有的请求都进入DB</p>
<h3 id="解决方法-2">解决方法</h3>
<ol>
<li>缓存高可用</li>
<li>DB限流</li>
<li>服务降级</li>
</ol>
<h1 id="redis的过期策略以及内存淘汰机制">Redis的过期策略以及内存淘汰机制</h1>
<p>定期删除+惰性删除策略<br>
定期抽样删除，如果使用这个key的时候去判断，是不是过期了</p>
<h1 id="单线程的redis为什么这么快">单线程的redis为什么这么快</h1>
<h2 id="纯内存操作">纯内存操作</h2>
<h2 id="单线程操作避免了频繁的上下文切换">单线程操作，避免了频繁的上下文切换</h2>
<h2 id="采用了非阻塞io多路复用机制">采用了非阻塞I/O多路复用机制</h2>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Spring]]></title>
        <id>https://zwangsheng.github.io/post/spring/</id>
        <link href="https://zwangsheng.github.io/post/spring/">
        </link>
        <updated>2020-12-22T13:38:31.000Z</updated>
        <content type="html"><![CDATA[<h1 id="springbean-的生命周期">SpringBean 的生命周期</h1>
<ol>
<li>实例化BeanFactoryPostProcessor实现类</li>
<li>执行BeanFactoryPostProcessor的postProcessorBeanFactory方法</li>
<li>实例化BeanPostProcessor实现类</li>
<li>实例化InstantiationAwareBeanPostProcessorAdapter的实现类</li>
<li>执行InstantiationAwareBeanPostProcessor的postProcessorBeforeInstantiation方法</li>
<li>执行Bean的构造器</li>
<li>执行InstantiationAwareBeanPostProcessor的postProcessorPropertyValues方法</li>
<li>为Bean注入属性</li>
<li>调用BeanNameAware的setBeanName方法</li>
<li>调用BeanFactoryAware的setBeanFactory方法</li>
<li>执行BeanProcessor的postProcessBeforeInstantiation方法</li>
<li>调用InitializingBean的afterPropertyset方法</li>
<li>执行init-method方法</li>
<li>BeanPostProcessor的postProcessAfterInitialization方法</li>
<li>执行InstantiationAwareBeanPostProcessor的postProcessAfterInitialization方法</li>
<li>正常可以调用</li>
<li>DiposibleBean的destory方法</li>
<li>执行destory-method方法</li>
</ol>
<h1 id="spring-ioc">Spring IOC</h1>
<p>控制反转的思想，将程序原本直接创建对象的权利转交给Spring框架</p>
<p>IOC容器来为我们注入我们需要的对象</p>
<h1 id="spring-aop">Spring AOP</h1>
<p>面向切面编程，将那些业务无关，但是被业务需要的代码封装起来，降低耦合</p>
<p>AOP基于动态代理，实现了接口就用JDK动态代理，未实现的就用CGLIB来代理</p>
<h1 id="springbean-的作用域">SpringBean 的作用域</h1>
<h2 id="singleton">Singleton</h2>
<p>单例，默认单例</p>
<h2 id="prototype">Prototype</h2>
<p>每次请求这个bean都会产生一个新的bean</p>
<h2 id="request">Request</h2>
<p>每一次Http请求就会产生新的bean，仅在http request有效</p>
<h2 id="session">Session</h2>
<p>每一次Http请求就会产生新的bean，仅在http session有效</p>
<h1 id="bean-和-component的区别">@Bean 和 @Component的区别</h1>
<ol>
<li>作用对象不同<br>
@Component作用于对象，@Bean作用于方法</li>
<li>使用场景<br>
@Component一般是扫描的时候标注进行自动装配，@Bean声明这个方法产生bean</li>
</ol>
<h1 id="spring的隔离级别">Spring的隔离级别</h1>
<h2 id="isolation_default">ISOLATION_DEFAULT</h2>
<h2 id="isolation_read_uncommitted">ISOLATION_READ_UNCOMMITTED</h2>
<h2 id="isolation_read_committed">ISOLATION_READ_COMMITTED</h2>
<h2 id="isolation_repeatable_read">ISOLATION_REPEATABLE_READ</h2>
<h2 id="isolation_serializable">ISOLATION_SERIALIZABLE</h2>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[MySQL]]></title>
        <id>https://zwangsheng.github.io/post/mysql/</id>
        <link href="https://zwangsheng.github.io/post/mysql/">
        </link>
        <updated>2020-12-22T13:38:12.000Z</updated>
        <content type="html"><![CDATA[<h1 id="mysql的处理层">MySql的处理层</h1>
<ol>
<li>判断是否是查询语句，首先会查询缓存是否有了相对应的结果，有就直接返回，无就进入下一步</li>
<li>解析查询语句，创建一个解析树</li>
<li>优化sql语句</li>
</ol>
<h2 id="sql语句解析">SQL语句解析</h2>
<p><img src="https://zwangsheng.github.io/post-images/1610004870278.jpg" alt="" loading="lazy"><br>
<img src="https://zwangsheng.github.io/post-images/1610005256391.jpg" alt="" loading="lazy"></p>
<h1 id="执行引擎">执行引擎</h1>
<h2 id="innodb">Innodb</h2>
<p>事务存储型引擎，完全支持ACID特性，支持行级锁</p>
<h2 id="myisam">MyISAM</h2>
<p>非事务存储，适合只读类应用</p>
<h3 id="innodb和myisam的区别">Innodb和MyISAM的区别</h3>
<figure data-type="image" tabindex="1"><img src="https://zwangsheng.github.io/post-images/1610005550915.jpg" alt="" loading="lazy"></figure>
<h2 id="csv">CSV</h2>
<p>所有列都不能为null，可以直接在文本上进行修改，不支持索引</p>
<h1 id="锁">锁</h1>
<h2 id="表锁">表锁</h2>
<p>开销小，上锁快，不会出现死锁<br>
锁粒度大，发生冲突的概率高，并发度低</p>
<h2 id="行锁">行锁</h2>
<p>开销大，上锁慢，会出现死锁<br>
锁粒度小，发送冲突的概率小，并发度高</p>
<h2 id="间隙锁">间隙锁</h2>
<p>介于表锁和行锁之间</p>
<h3 id="行锁表锁使用场景的讨论">行锁表锁使用场景的讨论</h3>
<p>表锁适合以查询为主，只有少量按照索引进行更新<br>
行锁适合大量的按索引条件并发更新的不同数据，拥有并发查询的能力</p>
<h3 id="innodb的上锁">Innodb的上锁</h3>
<p>lock in share mode 读锁<br>
for update 写锁<br>
两个事务不能锁同一个索引<br>
insert ，delete ， update 在事务中都会自动默认加上排它锁<br>
行锁只有遇到索引才能成为行锁，不然升级成为表锁</p>
<h1 id="事务">事务</h1>
<h2 id="事务的特性">事务的特性</h2>
<h3 id="原子性">原子性</h3>
<p>操作要么全部成功要么全部失败</p>
<h3 id="一致性">一致性</h3>
<p>操作前后的结果保持一致</p>
<h3 id="隔离性">隔离性</h3>
<p>事务与事务之间保持隔离</p>
<h3 id="持久性">持久性</h3>
<p>事务一旦提交就被持久化</p>
<h2 id="事务的并发问题">事务的并发问题</h2>
<h3 id="脏读">脏读</h3>
<p>事务A读取事务B的数据，事务B进行回滚，导致A读到了脏数据</p>
<h3 id="不可重复读">不可重复读</h3>
<p>事务A多次读取相同的数据，事务B在这个过程中修改了数据，导致多次读取不一致</p>
<h3 id="幻读">幻读</h3>
<p>事务A多次读取大量数据，事务B往中间查了新的数据</p>
<h2 id="事务的隔离级别">事务的隔离级别</h2>
<h3 id="读未提交">读未提交</h3>
<p>读取未提交的事务的数据 --写允许读</p>
<h3 id="读已提交">读已提交</h3>
<p>读取已提交的事务的数据 --写静止读</p>
<h3 id="可重复读">可重复读</h3>
<p>事务开启之后，不允许写操作 --读静止写</p>
<h3 id="串行化">串行化</h3>
<p>所有事务串行化</p>
<h1 id="业务设计">业务设计</h1>
<h2 id="三大范式">三大范式</h2>
<ol>
<li>所有字段值都不可再分</li>
<li>表中的每个数据都要与主键相关</li>
<li>都与主键之间相关，而不是间接相关</li>
</ol>
<h2 id="反范式设计">反范式设计</h2>
<p>添加冗余来避免热点数据的连表查询</p>
<h1 id="索引">索引</h1>
<h2 id="聚集索引">聚集索引</h2>
<p>索引项的排序方式和表中记录的排序方式一样</p>
<p>一张表只能有一个聚集索引</p>
<h3 id="使用场合">使用场合</h3>
<p>返回的字段按照这个排序<br>
查询结果返回一个区间的值<br>
查询结果返回某值相同的大量结果集</p>
<h2 id="非聚集索引">非聚集索引</h2>
<h3 id="使用场合-2">使用场合</h3>
<p>唯一性比较高的时候<br>
获得的数据量少的情况</p>
<h2 id="索引失效的情况">索引失效的情况</h2>
<ol>
<li>like的%在前面</li>
<li>使用索引列进行计算</li>
<li>对索引使用了函数</li>
<li>对索引使用了!=</li>
</ol>
<h1 id="存储结构">存储结构</h1>
<h2 id="b-tree">B-Tree</h2>
<p>所有的键值分布在这个树的每个节点中</p>
<p>搜索可能在非叶子节点结束</p>
<p>所有的叶子节点在同一层，按照升序排列</p>
<h2 id="btree">B+Tree</h2>
<p>树的非叶子节点不存放数据</p>
<p>叶子节点存储数据和关键词，非叶子节点的关键字也会沉到叶子节点进行排序</p>
<p>叶子节点形成双向环状链表</p>
<h1 id="执行计划">执行计划</h1>
<p>帮助分析优化SQL语句</p>
<h2 id="详解">详解</h2>
<h3 id="id列">ID列</h3>
<p>表示查询select子句的顺序</p>
<p>id相同时，从上到下执行</p>
<p>id不同时，id大的先执行</p>
<h3 id="select_type列">Select_Type列</h3>
<p>区别普通查询，联合查询，子查询<br>
<img src="https://zwangsheng.github.io/post-images/1610011703036.jpg" alt="" loading="lazy"></p>
<h3 id="table列">Table列</h3>
<p>表示数据从哪张表来</p>
<h3 id="type列">Type列</h3>
<p>访问类型，衡量sql好坏的指标<br>
<em>system</em> &gt; <em>const</em> &gt; <em>eq_ref</em> &gt; <em>ref</em> &gt; fulltext &gt; ref_or_null &gt; index_merge &gt;unique_subquery &gt; index_subquery &gt; <em>range</em> &gt; <em>index</em> &gt; <em>ALL</em></p>
<h4 id="system">System</h4>
<p>表里只有一条数据</p>
<h4 id="const">Const</h4>
<p>通过索引一次就找到了</p>
<h4 id="eq_ref">eq_ref</h4>
<p>唯一性索引扫描</p>
<h4 id="ref">ref</h4>
<p>非唯一性索引扫描</p>
<h4 id="range">range</h4>
<p>只检索给定范围的行，使用一个索引来选择行</p>
<h4 id="index">index</h4>
<p>查询结果全是索引列的时候，虽然查了全表，但是也只是索引</p>
<h4 id="all">ALL</h4>
<p>遍历全表进行查询</p>
<h3 id="possible_keys">possible_keys</h3>
<p>可能使用的索引</p>
<h3 id="key">Key</h3>
<p>实际使用的索引</p>
<h3 id="key_len">Key_len</h3>
<p>表示索引使用的字节数</p>
<h3 id="ref-2">Ref</h3>
<p>显示索引的那一列被使用了</p>
<h3 id="rows">Rows</h3>
<p>估算找到记录所需要读取的记录条数</p>
<h3 id="extra">Extra</h3>
<p>特殊信息<br>
<img src="https://zwangsheng.github.io/post-images/1610012288249.jpg" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Epoll高效原理解析]]></title>
        <id>https://zwangsheng.github.io/post/epoll-gao-xiao-yuan-li-jie-xi/</id>
        <link href="https://zwangsheng.github.io/post/epoll-gao-xiao-yuan-li-jie-xi/">
        </link>
        <updated>2020-12-15T03:50:46.000Z</updated>
        <content type="html"><![CDATA[<p>进程调用epoll_Create方法的时候，linux内核会创建出一个eventpoll的结构体，在内核的cache当中建立一个红黑树存储epoll_ctx传输过来的socket，还会建立一个rdllist双向链表，存放准备就绪的事件。<br>
调用epoll_select的时候，判断rdllist是否为空即可，有数据就返回，没有就sleep，时间到了也返回。<br>
同时，所有添加到epoll的事件都会和外设建立回调的关系，也就是外设感应到事件就会调用这些回调函数，<br>
叫做ep_poll_callback，这样子的事件也会被放在rdllist当中。<br>
当调用epoll_wait检查有无发生事件的连接的时候，只检查rdllist有无epitem元素，如果不为空，就复制时间到用户空间（可以使用内核用户共享内存提高效率），同时返回数量给用户。<br>
<img src="https://zwangsheng.github.io/post-images/1608004833881.jpg" alt="" loading="lazy"><br>
将功能进行拆分<br>
select当中维护等待队列和阻塞进程是一个函数，<br>
epoll进行拆分，拆分为epoll_ctl维护等待队列，epoll_wait阻塞进程<br>
维护就绪列表<br>
select在select阻塞的时候遍历一遍socket列表，发现有感兴趣事件之后交由进程处理，进程不知道哪个socket，所以需要两遍遍历socket列表<br>
epoll维护了rdllist，里面存放了就绪的socket免得遍历<br>
<img src="https://zwangsheng.github.io/post-images/1608010906772.jpg" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Netty]]></title>
        <id>https://zwangsheng.github.io/post/netty/</id>
        <link href="https://zwangsheng.github.io/post/netty/">
        </link>
        <updated>2020-12-14T08:28:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="计算机网络">计算机网络</h2>
<h3 id="tcpip模型">TCP/IP模型</h3>
<p><img src="https://zwangsheng.github.io/post-images/1607934666294.jpg" alt="" loading="lazy"><br>
<img src="https://zwangsheng.github.io/post-images/1607934809530.jpg" alt="" loading="lazy"></p>
<h3 id="mac地址-48位">MAC地址 -48位</h3>
<p>物理地址<br>
写在网卡里，算不可变的地址<br>
链路层</p>
<h3 id="ip地址-32位">IP地址 -32位</h3>
<p>逻辑地址<br>
互联网上的每一个网络和每一台主机配置一个唯一的逻辑地址<br>
网络层</p>
<h3 id="tcp">TCP</h3>
<p>端到端的通讯，面向连接<br>
在开始通讯前，要建立连接，完成通讯后关闭连接<br>
可靠的数据流服务</p>
<h4 id="超时重传机制">超时重传机制</h4>
<p>超时的时长并不是固定死的，通过网路情况动态调整，RRT</p>
<h4 id="应答确认机制">应答确认机制</h4>
<h4 id="滑动窗口">滑动窗口</h4>
<h4 id="三次握手">三次握手</h4>
<figure data-type="image" tabindex="1"><img src="https://zwangsheng.github.io/post-images/1607935548404.jpg" alt="" loading="lazy"></figure>
<ol>
<li>Client 将SYN标志位置1，产生随机数seq=j，将数据包发给Sever，Client进入SYN_SENT状态</li>
<li>Sever收到SYN为1的包，知道Client要建立连接，将标志位SYN和ACK都置1，ack=j+1，随机产生seq=k，发送给Client，Sever进入SYN_RCVD状态（sever确认自己的接收无误）</li>
<li>Client收到确认包，检查ack的值是否为j+1，ACK是否为1，如果正确的话（client确认自己的发送和接受无误），ACK置1，ack=k+1，将数据包传给Sever，Client进入ESTABLISHED，Sever检查ack的值是否为k+1，ACK是否为1，没问题的话（sever确认自己的发送无误），Sever进入ESTABLISHED，之后客户端和服务端可以开始数据传输</li>
</ol>
<h5 id="为什么一定要握手三次">为什么一定要握手三次</h5>
<p>因为TCP为了保证可靠传输，所以TCP通讯的双方都要维护序列号，三次是双方都可知序列号的最小次数。</p>
<h5 id="三次握手带来的洪泛攻击">三次握手带来的洪泛攻击</h5>
<p>攻击者假造ip地址发送给Sever端进行第一次握手，那么Sever端会进行回应，那么因为ip是伪造的，所以不会进行第三次的握手，Sever端的资源就会一直被握手所拖垮，有效的请求就很难获取到Sever的资源</p>
<h6 id="无效连接监视释放-对于所有的连接都进行监控到达一定的阀值之后断开">无效连接监视释放 对于所有的连接都进行监控，到达一定的阀值之后断开</h6>
<h6 id="延缓-tcb-分配方法-延缓资源的分配等真的建立了再分配">延缓 TCB 分配方法 延缓资源的分配，等真的建立了再分配</h6>
<h6 id="使用防火墙-防火墙确认连接的真实性之后才会发起真的syn">使用防火墙 防火墙确认连接的真实性之后才会发起真的SYN</h6>
<h4 id="四次挥手">四次挥手</h4>
<figure data-type="image" tabindex="2"><img src="https://zwangsheng.github.io/post-images/1607935554881.jpg" alt="" loading="lazy"></figure>
<ol>
<li>甲方主动执行关闭，发送FIN为1的给乙方，甲方进入FIN-WAIT-1</li>
<li>乙方收到FIN为1的报文，进入被动执行关闭，发出ACK为1的报文，乙方进入CLOSE-WAIT，甲方收到报文进入FIN-WAIT-2</li>
<li>乙方主动执行关闭，发送FIn为1的给甲方</li>
<li>甲方接受报文，并发出应答，进入TIME_WAIT阶段，经历2*MSL进入CLOSE状态</li>
<li>乙方接受报文，直接进入CLOSE状态</li>
</ol>
<h5 id="为什么挥手要四次">为什么挥手要四次</h5>
<p>TCP是全双工的，需要双方都关闭读写</p>
<h5 id="为什么需要time_wait状态">为什么需要TIME_WAIT状态</h5>
<ol>
<li>保证可靠的终止TCP连接 - ack报文丢失，重发fin报文</li>
<li>保证让迟来的TCP报文有足够的时间被识别并丢弃 - 给足够的时间关闭报文，linux的端口复用</li>
</ol>
<h3 id="http">HTTP</h3>
<h4 id="一次完整的http请求">一次完整的HTTP请求</h4>
<ol>
<li>进行DNS域名解析（本地浏览器缓存，操作系统缓存，hosts文件，域名服务器）</li>
<li>TCP三次握手</li>
<li>客户端发起HTTP请求</li>
<li>服务端响应HTTP请求</li>
<li>客户端解析HTML代码，请求资源</li>
<li>客户端渲染</li>
<li>关闭TCP连接</li>
</ol>
<h4 id="http请求报文">HTTP请求报文</h4>
<figure data-type="image" tabindex="3"><img src="https://zwangsheng.github.io/post-images/1607938293504.jpg" alt="" loading="lazy"></figure>
<h4 id="http响应报文">HTTP响应报文</h4>
<figure data-type="image" tabindex="4"><img src="https://zwangsheng.github.io/post-images/1607938304215.jpg" alt="" loading="lazy"></figure>
<h2 id="java原生网络">Java原生网络</h2>
<h3 id="bio">BIO</h3>
<p>由一个独立的Acceptor线程处理连接，接受请求以后，产生新的线程处理，处理完成后，返回应答，销毁线程（一请求一应答模型）数据的读入和写出都阻塞在一个线程里<br>
面向流 需要读取完流内数据，不能前后移动<br>
阻塞 直到read或者wirte完之前不能处理其他事</p>
<h3 id="nio">NIO</h3>
<p>面向缓冲区 数据总是从channel到buffer或者从buffer到channel<br>
非阻塞</p>
<h4 id="nio架构">NIO架构</h4>
<figure data-type="image" tabindex="5"><img src="https://zwangsheng.github.io/post-images/1607940889871.jpg" alt="" loading="lazy"></figure>
<h4 id="selector-选择器">Selector 选择器</h4>
<p>一个线程控制Selector可以监控多个channel</p>
<h4 id="channel-通道">Channel 通道</h4>
<p>通道中的数据总是要先读到一个 Buffer，或者总是要从一个 Buffer 中写入</p>
<h4 id="buffer">Buffer</h4>
<p>将数据写入缓冲，再通过通道把缓冲的数据发送出去，读也是一样， 数据总是先从通道读到缓冲，应用程序再读缓冲的数据</p>
<h5 id="capacity-容量">capacity 容量</h5>
<h5 id="position-写模式下代表当前可写的位置读模式代表当前可读的位置">position 写模式下，代表当前可写的位置，读模式代表当前可读的位置</h5>
<h5 id="limit-写模式下代表还能写容量读模式代表当前最大可读的位置">limit 写模式下，代表还能写容量，读模式代表当前最大可读的位置</h5>
<h5 id="直接内存">直接内存</h5>
<p>HeapByteBuffer直接分配的在堆上，flush到远程的时候还需要copy到直接内存<br>
直接内存可能会造成OOM<br>
申请空间耗费性能，但是IO操作要优于堆内存</p>
<h5 id="向buffer写内容">向Buffer写内容</h5>
<ol>
<li>从Channel读取到Buffer</li>
<li>通过Buffer的put方法读取到Buffer</li>
</ol>
<h5 id="从buffer读取内容">从Buffer读取内容</h5>
<ol>
<li>从Buffer读取到Channel</li>
<li>通过get方法从Buffer读取</li>
</ol>
<h5 id="flip">flip()</h5>
<p>把Buffer从读模式切换到写模式，postion设为0，limit设置为postion</p>
<h5 id="clear">clear()</h5>
<p>清除整个缓冲区<br>
postion为0，limit变为caption</p>
<h5 id="rewind">rewind()</h5>
<p>postion设为0，limit还是旧值，可以从头开始读取数据</p>
<h5 id="compact">compact()</h5>
<p>清除读过的缓冲区<br>
将未读数据拷贝到开始的地方，postion变为未读的结尾，limit变为caption</p>
<h5 id="equals">equals()</h5>
<p>只比较剩余的数据</p>
<h4 id="selectionkey">SelectionKey</h4>
<p>selectionKey 代表channel和selector建立的关系<br>
channel移除selectionKey的时候，取消的selectionKey并不是直接从selector中移除，而是添加到cancelledKeys当中，等到下次操作才会移除他<br>
<img src="https://zwangsheng.github.io/post-images/1607941741704.jpg" alt="" loading="lazy"></p>
<h3 id="rpc">RPC</h3>
<h4 id="rpc调用流程">RPC调用流程</h4>
<ol>
<li>Client以本地的方式使用存根</li>
<li>Client stub 收到使用之后，负责把类名，方法名，参数包装，通过网络发送给Sever</li>
<li>Sever收到消息以后，将代理存根解码之</li>
<li>Server stub调用解码后的数据，进行本地调用，将结果编码发送给Client stub</li>
<li>Client stub收到消息，解码，给Client结果</li>
</ol>
<h4 id="实现rpc需要解决的问题">实现RPC需要解决的问题</h4>
<ol>
<li>代理问题</li>
<li>序列化问题</li>
<li>通信问题</li>
<li>服务的实例化</li>
<li>服务的上下线的可知性</li>
</ol>
<h3 id="reactor-模式">Reactor 模式</h3>
<h4 id="单线程模式的reactor">单线程模式的Reactor</h4>
<ol>
<li>注册一个Acceptor时间处理器到Reactor中，监听客户端向服务端发起的连接请求</li>
<li>Reactor监听到ACCEPT事件，交由Acceptor处理，Acceptor处理器通过accpet获得对应的连接，然后该链接会关注READ事件以及对READ事件的处理方法</li>
<li>当Reactor监听到read和write事件会交由对应的处理器处理</li>
<li>处理完所有IO之后，会执行select方法阻塞到下个事件到来<br>
<img src="https://zwangsheng.github.io/post-images/1607948794153.jpg" alt="" loading="lazy"></li>
</ol>
<h4 id="单线程模式的reactor工作线程池">单线程模式的Reactor+工作线程池</h4>
<p>非IO操作的从Reactor线程中移出交由工作线程池执行<br>
<img src="https://zwangsheng.github.io/post-images/1607948802093.jpg" alt="" loading="lazy"></p>
<h4 id="多reactor线程">多Reactor线程</h4>
<p>每一个Reactor都有自己的Selector，线程池，分发事件循环逻辑<br>
mainReactor只有一个，但是subReactor一般有多个<br>
mainReactor线程负责接受客户端的连接请求，然后将接受到的SocketChannel传递给subReactor，有Reactor来负责进行通讯</p>
<ol>
<li>注册Acceptor事件处理器到mainReactor中，Acceptor关注ACCEPT事件，mainReactor监听连接请求</li>
<li>客户端发起请求，Acceptor监听到请求，开启SocketChannel，并传递给subReactor</li>
<li>subReactor线程池分配一个subReator给这个SocketChannel，将Read事件和处理器注册到subReacotr当中</li>
<li>当IO事件就绪，IO线程有subReactor处理，业务逻辑放到线程池处理<br>
<img src="https://zwangsheng.github.io/post-images/1607949267608.jpg" alt="" loading="lazy"></li>
</ol>
<h4 id="观察者模式和reacotr的区别">观察者模式和Reacotr的区别</h4>
<p>观察者模式主要是一个或者多个对象依赖于一个对象，当对象改变的时候进行通知<br>
区别在于观察者关注一个事件，Reactor关注多个事件</p>
<h2 id="netty">Netty</h2>
<h3 id="内置通讯方式">内置通讯方式</h3>
<ol>
<li>NIO nio</li>
<li>EPOLL 基于linux，完全非阻塞</li>
<li>OIO 阻塞流</li>
<li>Local VM内部通道</li>
<li>Embedded 单元测试</li>
</ol>
<h3 id="为什么要用netty">为什么要用Netty</h3>
<ol>
<li>Java nio虽然提供多路复用IO，但是没有很好的封装</li>
<li>NIO的类库太过复杂</li>
<li>一个好的网络程序还需要很多上层的特殊服务</li>
<li>Java nio存在空轮训的bug</li>
</ol>
<h3 id="channel">Channel</h3>
<p>代表一个到实体的开放连接，打开和关闭可以被看作连接和断开连接<br>
为了保证顺序，所以实现了CompComparable接口，如果不同的channel返回相同的hashCode就回抛错</p>
<h4 id="channel的生命周期">Channel的生命周期</h4>
<ol>
<li>ChannelUnregistered Channel被创建，但是未注册到EventLoop上</li>
<li>ChannelRegistered Channel被注册到EventLoop上</li>
<li>ChannelActive Channel处于活动状态，已经连接到远程节点上了，可以进行传输数据</li>
<li>ChannelInactive Channel未连接到远程节点</li>
</ol>
<h3 id="eventloop">EventLoop</h3>
<p>可以看作一个线程<br>
控制流，多线程处理，并发<br>
用于处理网络连接生命周期内所发生的事件<br>
在Netty4，所有的IO操作和事件都会被分配给EventLoop的Thread执行<br>
一旦一个Channel被分配给一个EventLoop，那么在Channel的生命周期内都会使用这个EventLoop的线程</p>
<h3 id="eventloopgroup">EventLoopGroup</h3>
<p>看作一个线程组<br>
通过round-robin顺序循环为Channel分配EventLoop达到一个均衡的分配</p>
<h3 id="channelhandler">ChannelHandler</h3>
<p>充当入站或者出出站数据逻辑的容器</p>
<h4 id="channelhandler的生命周期">ChannelHandler的生命周期</h4>
<ol>
<li>handlerAdded 当把 ChannelHandler 添加到 ChannelPipeline 中时被调用 handlerRemoved 当从</li>
<li>ChannelPipeline 中移除 ChannelHandler 时被调用</li>
<li>exceptionCaught 当处理过程中在 ChannelPipeline 中有错误产生时被调用</li>
</ol>
<h4 id="channelinboundhandler">ChannelInboundHandler</h4>
<p>处理入站数据以及各种状态变化</p>
<h5 id="channelinboundhandler响应事件">ChannelInboundHandler响应事件</h5>
<ol>
<li>channelRegistered 当 Channel 已经注册到它的 EventLoop 并且能够处理 I/O 时被调用</li>
<li>channelUnregistered 当 Channel 从它的 EventLoop 注销并且无法处理任何 I/O 时被调<br>
用</li>
<li>channelActive 当 Channel 处于活动状态时被调用;Channel 已经连接/绑定并且已经就 绪</li>
<li>channelInactive 当 Channel 离开活动状态并且不再连接它的远程节点时被调用</li>
<li>channelReadComplete 当 Channel 上的一个读操作完成时被调用</li>
<li>channelRead 当从 Channel 读取数据时被调用</li>
<li>ChannelWritabilityChanged 当 Channel 的可写状态发生改变时被调用 可以调整阀值</li>
<li>userEventTriggered 当 ChannelnboundHandler.fireUserEventTriggered()方法被调用时被 调用</li>
</ol>
<h4 id="channeloutboundhandler">ChannelOutboundHandler</h4>
<p>处理出站数据并且允许拦截所有的操作</p>
<h5 id="channeloutboundhandler响应事件">ChannelOutboundHandler响应事件</h5>
<ol>
<li>bind(ChannelHandlerContext,SocketAddress,ChannelPromise) 当请求将 Channel 绑定到本地地址时被调用</li>
<li>connect(ChannelHandlerContext,SocketAddress,SocketAddress,ChannelPromise) 当请求将 Channel 连接到远程节点时被调用</li>
<li>disconnect(ChannelHandlerContext,ChannelPromise) 当请求将 Channel 从远程节点断开时被调用</li>
<li>close(ChannelHandlerContext,ChannelPromise) 当请求关闭 Channel 时被调用</li>
<li>deregister(ChannelHandlerContext,ChannelPromise) 当请求将 Channel 从它的 EventLoop 注销时被调用</li>
<li>read(ChannelHandlerContext) 当请求从 Channel 读取更多的数据时被调用</li>
<li>flush(ChannelHandlerContext) 当请求通过 Channel 将入队数据冲刷到远程节点时被调 用</li>
<li>write(ChannelHandlerContext,Object,ChannelPromise) 当请求通过 Channel 将数据写到 远程节点时被调用</li>
</ol>
<h4 id="netty资源管理">Netty资源管理</h4>
<p>Write时，如果确保数据被发送网络，Netty会自己释放Buffer，但是如果OutHandler处理了write方法并且丢弃了数据，那么需要自己进行释放<br>
Read时，如果确保每个InHandler向后传递了数据，那么Netty也会帮忙释放数据，但是如果处理数据却没有后传数据，需要自己释放<br>
这些都有可能会造成内存泄漏</p>
<h4 id="simplechannelinboundhandler">SimpleChannelInboundHandler</h4>
<p>数据被channelRead0方法消费之后自动释放<br>
所以在我们实现业务Inhandler的时候要注意，要么继续向后传递，要么原地释放</p>
<h3 id="channelpipeline">ChannelPipeline</h3>
<p>Channel被创建的时候会关联一个ChannelPipeline，这个关联是永久的<br>
事件经过ChannelPipeline是ChannelHandler的工作，他们的执行顺序是由他们被添加的顺序所决定的<br>
消息或者入站事件被读取，那么会从ChannelPipeline的头部开始流动，到达尾部就处理完毕<br>
出站事件和消息，会从尾部流向头部，最终达到网络，通常会触发一个写操作<br>
<img src="https://zwangsheng.github.io/post-images/1607957231284.jpg" alt="" loading="lazy"></p>
<h3 id="channelhandlercontext">ChannelHandlerContext</h3>
<p>ChannelHandlerContext代表ChannelHandler和ChannelPipeline的关系<br>
每当有ChannelHandler被添加进ChannelPipeline都会创建一个ChannelHandlerContext<br>
主要功能是管理它所关联的Channelhandler在和同一个ChannelPipeline其他ChannelHandler的交互<br>
有很多方法，有一些相同的方法也会存在Channel和ChannelPipeline当中，但是有很重要的不同，Channel或者ChanelPipeline的方法会从头走到尾，ChannelHandlerContext的方法会从自己管理的ChannelHandler开始，然后只会传播给上一个或者下一个能处理本时间的ChannelHandler<br>
<img src="https://zwangsheng.github.io/post-images/1607957787166.jpg" alt="" loading="lazy"></p>
<h3 id="channeloption">ChannelOption</h3>
<h4 id="channeloptionso_backlog-指定连接等待队列的大小">ChannelOption.SO_BACKLOG 指定连接等待队列的大小</h4>
<p>服务端处理客户端的请求是按照顺序处理的，暂时处理不了的放进队列</p>
<h4 id="channeloptionso_reuseaddr-允许重复使用本地地址和端口">ChannelOption.SO_REUSEADDR 允许重复使用本地地址和端口</h4>
<p>一个进程非正常退出后，内核需要一定的时间释放端口，设置这个可以正常使用端口</p>
<h4 id="channeloptionso_keepalive-测试连接的状态">ChannelOption.SO_KEEPALIVE 测试连接的状态</h4>
<p>并不是长链接，一般是长时间之后，进行发送探活报文</p>
<h4 id="channeloptionso_sndbuf-接受缓冲区大小">ChannelOption.SO_SNDBUF 接受缓冲区大小</h4>
<p>保存接受的数据，直到读取成功</p>
<h4 id="channeloptionso_rcvbuf-发送缓冲区大小">ChannelOption.SO_RCVBUF 发送缓冲区大小</h4>
<p>保存发送数据，直到发送完成</p>
<h4 id="channeloptionso_linger-阻塞close方法确保数据发送完毕">ChannelOption.SO_LINGER 阻塞close方法，确保数据发送完毕</h4>
<p>linux内核遇到close方法，会尽量发送数据，但不确保发送完全，这个参数可以阻塞close方法，直到发送完</p>
<h4 id="channeloptiontcp_nodelay-用于小数据即时发送一定程度上解决粘包">ChannelOption.TCP_NODELAY 用于小数据即时发送，一定程度上解决粘包</h4>
<p>Nagle会判断数据包太小的时候，等待封装成足够大的包再进行发送，提高了网络的负载，但是会造成延迟发送</p>
<h3 id="channelfuture">ChannelFuture</h3>
<p>执行异步操作的结果返回<br>
可以添加ChannelFutureListener，以便在这个操作完成的时候得到通知，无论成功还是失败</p>
<h3 id="bytbuf">BytBuf</h3>
<h4 id="优点">优点</h4>
<ol>
<li>可以被用户自定义的缓冲区扩展</li>
<li>通过内置的复合缓冲区实现了透明的零拷贝</li>
<li>容量可以按需扩展</li>
<li>不需要flip方法进行读写模式的切换</li>
<li>读写用了不同的索引</li>
<li>支持方法的链式调用</li>
<li>支持引用计数</li>
<li>支持池化</li>
</ol>
<h4 id="使用模式">使用模式</h4>
<h5 id="堆缓冲区">堆缓冲区</h5>
<p>数据存储在JVM运行时数据区，能提供没有池化最快的分配和释放，通过检查是否由数组支撑来判断是不是堆缓冲区</p>
<h5 id="直接缓冲区">直接缓冲区</h5>
<p>基于堆外内存，分配和释放代价较大</p>
<h5 id="复合缓冲区">复合缓冲区</h5>
<p>将多个ByteBuf整合成一个，进行统一的Api管理</p>
<h4 id="分配">分配</h4>
<h5 id="bytebufallocator">ByteBufAllocator</h5>
<p>通过ByteBufAllocator的方法获得不同类型的ByteBuf<br>
Channel或者ChannelHandlerContext<br>
通过alloc获得不同类型的byteBuf<br>
Nettyti gong提供两种ByteBufAllocator的实现</p>
<h6 id="pooledbytebufallocator-池化-默认实现">PooledByteBufAllocator 池化 默认实现</h6>
<p>池化实例，提高内存的利用率</p>
<h6 id="unpooled-bytebufallocator-非池化">Unpooled-ByteBufAllocator 非池化</h6>
<h5 id="unpooled">Unpooled</h5>
<p>提供静态方法来提供创建非池化的ByteBuf<br>
每一次都整个实例，方便</p>
<h6 id="get-set">get set</h6>
<p>从给定索引开始，不会移动索引的进行获取数据</p>
<h6 id="read-write">read write</h6>
<p>从给索引开始，会移动索引</p>
<h5 id="可丢弃字节">可丢弃字节</h5>
<p>已经读过的字节，可以通过discardReadBytes，丢弃他们回收空间<br>
调用之后，原来的空就是可写空间<br>
频繁的调用确实可以保证可写分段的最大化，但是这可能造成内存的复制</p>
<h5 id="可读字节">可读字节</h5>
<p>默认为0<br>
<img src="https://zwangsheng.github.io/post-images/1607960282036.jpg" alt="" loading="lazy"></p>
<h5 id="可写字节">可写字节</h5>
<p>默认为0，从当前的writeIndex开始增加<br>
<img src="https://zwangsheng.github.io/post-images/1607960364203.jpg" alt="" loading="lazy"></p>
<h5 id="索引管理">索引管理</h5>
<p>通过mark和rest方法来标记和重置<br>
也可以通过index参数进行移动索引<br>
clear将索引置0但是不会清除数据</p>
<h5 id="查找操作">查找操作</h5>
<p>indexOf<br>
forEachByte</p>
<h5 id="派生缓冲区">派生缓冲区</h5>
<p>拥有自己的索引，内部数据共享</p>
<h5 id="引用计数">引用计数</h5>
<h5 id="工具类">工具类</h5>
<p>equals 判断ByteBuf实例的相等性<br>
hexdump 十六进制打印ByteBuf的内容</p>
<h5 id="资源释放">资源释放</h5>
<ol>
<li>入站，使用mpleChannelInboundHandler，写 channelRead()方法使用ReferenceCountUtil.release()，使用 ctx.fireChannelRead 继续向后传递</li>
<li>出站，当调用了 write 或者 writeAndFlush 方法后，Netty 会自动替我们释放，不需要我们业务代码自行释放</li>
</ol>
<h4 id="tcp粘包半包">TCP粘包/半包</h4>
<p>客户端发送D1，D2两个包，因为不确定服务端的接收大小，所以会出现四种情况</p>
<ol>
<li>服务端分两次收到独立的数据包，分别是D1，D2，没有粘包和拆包</li>
<li>服务端一次性收到一个数据包，D1，D2粘合在一起，被称为TCP粘包</li>
<li>服务端收到两个包，但D1，D2一部分粘在一起，被成为TCP拆包</li>
<li>如果服务端的滑动窗口很小，可能发送多次收到D1，D2拆开的包</li>
</ol>
<h5 id="粘包原因">粘包原因</h5>
<p>客户端开启Nagle算法，对较小的数据包进行整合发送，服务端无法区分<br>
服务端收到之后写入缓冲区，在下一次取数据的时候取出多个包的数据</p>
<h5 id="半包原因">半包原因</h5>
<p>ip分片传输，传输过程中丢失部分数据，缓冲区大小</p>
<h5 id="解决办法">解决办法</h5>
<ol>
<li>在包尾添加特殊分隔符</li>
<li>消息定长，不够空位补空格</li>
<li>消息分为消息头和消息体，消息头包含消息总长度</li>
</ol>
<h5 id="lengthfieldbasedframe">LengthFieldBasedFrame</h5>
<h6 id="maxframelength-包的最大长度">maxFrameLength 包的最大长度</h6>
<h6 id="lengthfieldoffset-长度域的偏移量表示跳过多少字节之后是长度域">lengthFieldOffset 长度域的偏移量，表示跳过多少字节之后是长度域</h6>
<h6 id="lengthfieldlength-长度域的长度">lengthFieldLength 长度域的长度</h6>
<h6 id="lengthadjustment-长度域的修正值">lengthAdjustment 长度域的修正值</h6>
<h6 id="initialbytestostrip-数据帧当中跳过的字节数跳过多少的数据才是业务数据">initialBytesToStrip 数据帧当中跳过的字节数，跳过多少的数据才是业务数据</h6>
<h6 id="failfast-为true的话读取完长度域的长度就判断是否过长">failFast 为true的话读取完长度域的长度就判断是否过长</h6>
<h3 id="编码器">编码器</h3>
<p>MessageToByteEncoder<I><br>
MessageToMessageEncoder<T></p>
<h4 id="encodechannelhandlercontext-ctxi-msgbytebuf-out">encode(ChannelHandlerContext ctx,I msg,ByteBuf out)</h4>
<p>传入Imsg，然后被传给下个ChannelOutboundHandler</p>
<h3 id="解码器">解码器</h3>
<p>ByteToMessageDecoder<br>
MessageToMessageDecoder<T><br>
负责将入站数据从一种格式转化成另一种格式，所以实质上是ChannelInboundHandler</p>
<h4 id="decodechannelhandlercontext-ctxbytebuf-inlistobject-out">decode(ChannelHandlerContext ctx,ByteBuf in,List<Object> out)</h4>
<p>ByteBuf负责传入数据，List out负责添加编码信息<br>
对这个方法会进行重复调用，直到确定没有新元素加入list和ByteBuf没有更多的可读字节<br>
之后如果list不为空，那么list将被传递给下一个ChannelInboundHandler</p>
<h3 id="空闲时间检测">空闲时间检测</h3>
<h4 id="idlestatehandler">IdleStateHandler</h4>
<p>连接空闲时间长，触发IdleStateEvent<br>
重写InHandler userEventTriggered方法来处理 IdleStateEvent事件</p>
<h4 id="readtimeouthandler">ReadTimeoutHandler</h4>
<p>如果指定的时间间隔没有收到入站消息，就跑出ReadTimeOutExcption，并关闭对应的Channel</p>
<h4 id="writetimeouthandler">WriteTimeoutHandler</h4>
<p>如果指定时间没有写入消息，抛出WriteTimeOutExcption，并关闭对应的Channel</p>
<h4 id="java序列化">java序列化</h4>
<h5 id="java序列化的缺点">java序列化的缺点</h5>
<ol>
<li>无法跨语言</li>
<li>序列化之后流太大</li>
<li>序列化的性能较差</li>
</ol>
<h3 id="单元测试">单元测试</h3>
<figure data-type="image" tabindex="6"><img src="https://zwangsheng.github.io/post-images/1607999656762.jpg" alt="" loading="lazy"></figure>
<h3 id="udp广播和udp单播">UDP广播和UDP单播</h3>
<h3 id="udt">UDT</h3>
<h3 id="服务器推送技术">服务器推送技术</h3>
<h4 id="ajax">Ajax</h4>
<p>定时器不停的轮询</p>
<h4 id="comet">Comet</h4>
<h3 id="websocket通讯">WebSocket通讯</h3>
<p>设置<br>
Upgrade: websocket<br>
Connection: Upgrade<br>
<img src="https://zwangsheng.github.io/post-images/1608000009383.jpg" alt="" loading="lazy"><br>
<img src="https://zwangsheng.github.io/post-images/1608000241644.jpg" alt="" loading="lazy"></p>
<h3 id="http和https的区别">HTTP和HTTPS的区别</h3>
<p>https较于http安全<br>
http端口80，https端口443</p>
<h3 id="netty解决java空轮训bug">Netty解决java空轮训Bug</h3>
<p>空轮询的bug来自于poll和epoll对于突然中断的连接返回POLLHUP，POLLERR，eventSet的变化导致Selector被唤醒，执行select方法的时候返回是0，导致对Key值遍历的事件处理不了</p>
<h4 id="修复方法">修复方法</h4>
<h5 id="去除selectkeys然后刷新selector">去除SelectKeys，然后刷新Selector</h5>
<p>存在问题，多线程key.cannel可能于selectNow存在并发情况，测试后基本无效</p>
<h5 id="创建新的selector-netty办法">创建新的Selector netty办法</h5>
<p>旧selector无效轮询几次后，重建selector，把channel取消注册，然后注册到新的selector上</p>
<h3 id="netty的内存管理">Netty的内存管理</h3>
<p>申请一块大内存Arena，Arena由许多的chuck组成，每个chuck默认由2048个page组成<br>
Chuck通过AVL管理page，中间节点表示内存区域，节点记录他在内存区域当中的偏移量<br>
当page被分配出去后，中间节点会进行标记，代表节点下的区域被分配<br>
大于8k分配在poolChunkList<br>
小于8k分配在PoolSubpage，page分段的子页</p>
<h3 id="select-poll-epoll-的区别">select、poll、epoll 的区别</h3>
<h4 id="一个进程打开的连接数">一个进程打开的连接数</h4>
<p>select 单个进程所能打开的最大连接数有 FD_SETSIZE 宏定义<br>
poll 本质上和 select 没有区别，但是它没有最大连接数的限制，原因 是它是基于链表来存储的<br>
epoll 连接数基本上只受限于机器的内存大小</p>
<h4 id="fd-剧增后带来的-io-效率问题">FD 剧增后带来的 IO 效率问题</h4>
<p>select 线性遍历，随着增加会变慢<br>
poll 同上<br>
epoll 只有活跃的才会处理</p>
<h4 id="消息传递方式">消息传递方式</h4>
<p>select 内核需要将消息传递到用户空间，都需要内核拷贝动作<br>
poll 同上<br>
epoll 内核和用户公用一块空间</p>
<h3 id="水平触发和垂直触发">水平触发和垂直触发</h3>
<h4 id="水平触发-level_triggered">水平触发 Level_triggered</h4>
<p>当监控的FD上有读写事件发生的时候，epoll_wait()会通知程序取读写，如果上次没有读完，还是会继续通知，一直不读一直通知</p>
<h4 id="边缘触发-edge_triggered">边缘触发 Edge_triggered</h4>
<p>监控的FD上发生读写事件，epoll_wait()只会通知你一次</p>
<h3 id="零拷贝">零拷贝</h3>
<p>计算机执行操作时，CPU 不需要先将数据从某处内存 复制到另一个特定区域。这种技术通常用于通过网络传输文件时节省 CPU 周期和内存带宽</p>
<h4 id="传统传输">传统传输</h4>
<figure data-type="image" tabindex="7"><img src="https://zwangsheng.github.io/post-images/1608002514413.jpg" alt="" loading="lazy"></figure>
<h4 id="mmap">mmap</h4>
<figure data-type="image" tabindex="8"><img src="https://zwangsheng.github.io/post-images/1608002529379.jpg" alt="" loading="lazy"></figure>
<h4 id="sendfile">sendfile</h4>
<figure data-type="image" tabindex="9"><img src="https://zwangsheng.github.io/post-images/1608002542573.jpg" alt="" loading="lazy"></figure>
<h4 id="splice">splice</h4>
<figure data-type="image" tabindex="10"><img src="https://zwangsheng.github.io/post-images/1608002556424.jpg" alt="" loading="lazy"></figure>
<h2 id="linux网络模型">Linux网络模型</h2>
<h3 id="同步异步">同步异步</h3>
<p>关注的是调用方是否主动获取结果<br>
同步，主动等待<br>
异步，其它方式通知</p>
<h3 id="阻塞和非阻塞">阻塞和非阻塞</h3>
<p>主要关注的是等待结果返回调用方的状态<br>
阻塞，结果返回之前挂起<br>
非阻塞，结果返回之前还能干活</p>
<h3 id="阻塞io-同步">阻塞IO 同步</h3>
<figure data-type="image" tabindex="11"><img src="https://zwangsheng.github.io/post-images/1608002774899.jpg" alt="" loading="lazy"></figure>
<h3 id="非阻塞io-同步">非阻塞IO 同步</h3>
<figure data-type="image" tabindex="12"><img src="https://zwangsheng.github.io/post-images/1608003043278.jpg" alt="" loading="lazy"></figure>
<h3 id="io复用-同步">IO复用 同步</h3>
<figure data-type="image" tabindex="13"><img src="https://zwangsheng.github.io/post-images/1608003132506.jpg" alt="" loading="lazy"></figure>
<h3 id="信号驱动io-同步">信号驱动IO 同步</h3>
<figure data-type="image" tabindex="14"><img src="https://zwangsheng.github.io/post-images/1608003190401.jpg" alt="" loading="lazy"></figure>
<h3 id="异步io-异步">异步IO 异步</h3>
<figure data-type="image" tabindex="15"><img src="https://zwangsheng.github.io/post-images/1608003225802.jpg" alt="" loading="lazy"></figure>
<h3 id="五种对比">五种对比</h3>
<figure data-type="image" tabindex="16"><img src="https://zwangsheng.github.io/post-images/1608003271973.jpg" alt="" loading="lazy"></figure>
<h2 id="linux内核">Linux内核</h2>
<h3 id="中断">中断</h3>
<p>当设备上有数据到达的时候，会给CPU相关的引脚触发一个电压的变化，通知CPU进行处理</p>
<h3 id="软中断">软中断</h3>
<p>给一个二进制标志位通知中断</p>
<h3 id="内核收包">内核收包</h3>
<figure data-type="image" tabindex="17"><img src="https://zwangsheng.github.io/post-images/1608003552960.jpg" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[单例模式]]></title>
        <id>https://zwangsheng.github.io/post/dan-li-mo-shi/</id>
        <link href="https://zwangsheng.github.io/post/dan-li-mo-shi/">
        </link>
        <updated>2020-12-10T08:10:49.000Z</updated>
        <content type="html"><![CDATA[<h1 id="非线程安全的懒汉模式">非线程安全的懒汉模式</h1>
<pre><code class="language-java">package Singleton;

/**
 * 非线程安全的懒汉模式
 * @author:wangSheng
 * @date:2020/12/10 4:21 下午
 */
public class Singleton1 {
    /**
     * 启动快，用的时候才加载，无锁
     * 不是线程安全的
     */
    private static Singleton1 singleton = null;

    private Singleton1() {
    }

    public static Singleton1 getInstance() {
        if (singleton == null) {
            singleton = new Singleton1();
        }
        return singleton;
    }
}
</code></pre>
<h1 id="线程安全的懒汉">线程安全的懒汉</h1>
<pre><code class="language-java">package Singleton;

/**
 * 安全的懒汉
 * @author:wangSheng
 * @date:2020/12/10 4:22 下午
 */
public class Singleton2 {
    /**
     * 启动快，用的时候才加载，线程安全
     * 获得实例需要串行获得
     */
    private static Singleton2 singleton = null;

    private Singleton2() {
    }

    public static synchronized Singleton2 getInstance() {
        if (singleton == null) {
            singleton = new Singleton2();
        }
        return singleton;
    }
}
</code></pre>
<h1 id=""></h1>
<pre><code class="language-java">package Singleton;

/**
 * DCL懒汉
 * @author:wangSheng
 * @date:2020/12/10 4:24 下午
 */
public class Singleton3 {
    /**
     * 启动快，用的时候才加载，线程安全
     * 注意加上volatile，保证可见性
     */
    private volatile static Singleton3 singleton = null;

    private Singleton3() {

    }

    public static Singleton3 getInstance() {
        if(singleton == null) {
            synchronized (Singleton3.class) {
                if (singleton == null) {
                    singleton = new Singleton3();
                }
            }
        }
        return singleton;
    }
}
</code></pre>
<h1 id="饿汉模式">饿汉模式</h1>
<pre><code class="language-java">package Singleton;

/**
 * 饿汉模式
 * @author:wangSheng
 * @date:2020/12/10 4:28 下午
 */
public class Singleton4 {
    /**
     * 使用无需等待获取
     * 一开始就加载如果不用的话占据内存
     */
    private static Singleton4 singleton = new Singleton4();

    private Singleton4() {
    }

    public static Singleton4 getInstance() {
        return singleton;
    }
}
</code></pre>
<h1 id="holder模式">Holder模式</h1>
<pre><code class="language-java">package Singleton;

/**
 * @author:wangSheng
 * @date:2020/12/10 4:29 下午
 */
public class Singleton5 {
    /**
     * 只有被调用才会进行加载，static只会加载一次，由JVM保证安全
     */
    private static class SingletonHolder {
        private static final Singleton5 singleton = new Singleton5();
    }

    private Singleton5() {

    }

    public static Singleton5 getInstance() {
        return SingletonHolder.singleton;
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java线程在运行态究竟是内核态还是用户态？]]></title>
        <id>https://zwangsheng.github.io/post/java-xian-cheng-zai-yun-xing-tai-jiu-jing-shi-nei-he-tai-huan-shi-yong-hu-tai/</id>
        <link href="https://zwangsheng.github.io/post/java-xian-cheng-zai-yun-xing-tai-jiu-jing-shi-nei-he-tai-huan-shi-yong-hu-tai/">
        </link>
        <updated>2020-12-09T08:53:11.000Z</updated>
        <content type="html"><![CDATA[<p>🈚️解，一个软件层面的线程的用户态和内核态是不能确定的<br>
解析一下内核态和用户态<br>
首先，内核态和用户态只是CPU执行权限<br>
用户态切换到内核态的方式</p>
<ol>
<li>系统调用</li>
<li>操作系统层面的异常</li>
<li>外设的中断<br>
目前了解的就是第一部分，java调用方法，唤起native方法，对应到OS的函数，是线程进入内核态。<br>
ex.线程的park方法，对应OS的某个函数，切换到内核态<br>
内核态就是进程在内核空间运行的状态<br>
用户态就是进程在用户空间运行的状态</li>
</ol>
<h2 id="cpu的上下文">CPU的上下文</h2>
<p>CPU的上下文可以狭义的理解为CPU的寄存器和程序计数器<br>
（可以在JVM中找到对应的模拟，所以JVM可以看作虚拟的小型OS）</p>
<h2 id="cpu上下文的类型">CPU上下文的类型</h2>
<h3 id="进程上下文的切换">进程上下文的切换</h3>
<p>这与我们说的进程上下文切换不同，系统调用的进程上下文切换针对的是同一个进程<br>
可以理解为这是一种权限切换</p>
<ol>
<li>保存CPU寄存器中的用户态指令位</li>
<li>CPU寄存器更新为内核态的指令位</li>
<li>跳转到内核态运行任务</li>
<li>系统调度结束之后，恢复原先保存的用户态，切换回用户态，继续运行</li>
</ol>
<h4 id="进程上下文切换">“进程上下文切换”</h4>
<p>我们所谈到的进程上下文切换是多个进程进行切换的情况，它比上述的系统调用的上下文切换多一个步骤即为保存进程内部的虚拟资源<br>
发生“进程上下文切换”的情况</p>
<ol>
<li>CPU时间轮转</li>
<li>CPU资源（不单单是CPU时间）不足，进程被挂起</li>
<li>进程主动挂起自己</li>
<li>高优先级进程运行</li>
</ol>
<h3 id="线程上下文的切换">线程上下文的切换</h3>
<p>分场景<br>
如果两个线程属于不同的进程，那么切换就像进程上下文切换一样<br>
如果两个线程属于同一个进程，那么切换的时候，虚拟内存不动，只需要切换线程私有的资源即可</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[并发编程]]></title>
        <id>https://zwangsheng.github.io/post/bing-fa-bian-cheng/</id>
        <link href="https://zwangsheng.github.io/post/bing-fa-bian-cheng/">
        </link>
        <updated>2020-12-09T07:44:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="进程-进程是程序运行资源分配的最小单位">进程 进程是程序运行资源分配的最小单位</h2>
<h2 id="线程-线程是cpu调度的最小单位需要依赖进程存在">线程 线程是CPU调度的最小单位，需要依赖进程存在</h2>
<h2 id="并发-交替运行单位时间">并发 交替运行，单位时间</h2>
<h2 id="并行-同时进行">并行 同时进行</h2>
<h2 id="守护线程-一般是支持性线程做辅助工作当不存在非守护线程的线程时jvm自动结束">守护线程 一般是支持性线程，做辅助工作，当不存在非守护线程的线程时，JVM自动结束</h2>
<h2 id="cpu时间片轮转机制">CPU时间片轮转机制</h2>
<p>CPU将时间切片，给进程分配时间，如果时间片用尽，CPU将分配给其他进程，如果时间片没用尽就阻塞或结束，CPU也会进行转换<br>
因为进程的上下文切换需要消耗资源，所以存在时间片时长的问题<br>
如果时间片太小，那么每次做完都会进行上下文切换，CPU的时间很大一部分浪费在切换上<br>
如果时间片太长，交互请求会变差</p>
<h2 id="run">run()</h2>
<p>业务逻辑实现的地方，调用后不会开启新的线程来处理，当作一个普通方法使用</p>
<h2 id="start">start()</h2>
<p>让一个线程进入队列等待CPU，分配到CPU以后会执行run()方法，不可重复执行</p>
<h2 id="yield">yield()</h2>
<p>让当前线程让出CPU使用权，不会释放锁资源，让出后立刻进入就绪状态，可能被立刻选中</p>
<h2 id="sleep">sleep()</h2>
<p>线程进入休眠，不会释放锁资源</p>
<h2 id="join">join()</h2>
<p>把指定线程加入到当前线程，将交替运行的线程改为顺序进行<br>
B线程调用了A线程的join，直到A执行完，B才能继续执行</p>
<h2 id="wait">wait()</h2>
<p>线程进入WAITING状态，只有等待另外线程通知，被中断或者超时才会返回，会释放锁</p>
<h2 id="notify">notify()</h2>
<p>通知在对象上等待的一个线程，让其从wait上返回，如果获得锁就运行，没获得就进入WAITING状态</p>
<h2 id="notifyall">notifyAll()</h2>
<p>通知对象上等待的所有线程</p>
<h2 id="抢占式调度-系统通知">抢占式调度 系统通知</h2>
<h2 id="协同式调度-线程本身控制通知系统进行切换">协同式调度 线程本身控制，通知系统进行切换</h2>
<h2 id="线程共享">线程共享</h2>
<h3 id="synchronized">synchronized</h3>
<h4 id="对象锁">对象锁</h4>
<ol>
<li>同步代码块，锁住对象实例</li>
<li>加在方法上</li>
</ol>
<h4 id="类锁">类锁</h4>
<ol>
<li>同步代码块，锁住类</li>
<li>加在静态方法上</li>
</ol>
<h4 id="锁膨胀">锁膨胀</h4>
<h3 id="volatile">volatile</h3>
<p>保证不同线程对修饰变量的可见性，但不能保证原子性</p>
<h3 id="threadlocal">ThreadLocal</h3>
<p>ThreadLocal为所有线程提供变量副本<br>
每个线程内部维护了一个ThreadLocalMap，key 是 ThreadLocal的实例<br>
<img src="https://zwangsheng.github.io/post-images/1607567891869.jpg" alt="" loading="lazy"></p>
<h4 id="threadlocal可能存在的内存泄漏问题">ThreadLocal可能存在的内存泄漏问题</h4>
<p><img src="https://zwangsheng.github.io/post-images/1607568021890.jpg" alt="" loading="lazy"><br>
key 与 ThreadLocal实例是弱引用，一旦发生GC就会被回收<br>
那么ThreadLocalMap就回存在key为null的value，无法被访问到，可能会造成内存泄漏<br>
只有currentThread结束，才能被回收</p>
<h5 id="如果threadlocal的实例与key为强引用那么当栈上的threadlocalref不再指向threadlocal之后key与threadlocal还是强引用依然造成内存泄漏">如果ThreadLocal的实例与key为强引用，那么当栈上的ThreadLocalRef不再指向ThreadLocal之后，key与ThreadLocal还是强引用，依然造成内存泄漏</h5>
<h5 id="如果threadlocal的实例与key为弱引用那在接下来的setgetremove的过程中还有可能对key为null的value进行清除">如果ThreadLocal的实例与key为弱引用，那在接下来的set，get，remove的过程中还有可能对key为null的value进行清除</h5>
<h5 id="所以根源并不是弱引用而是threadlocalmap的生命周期和线程一样长如果不及时删除对应的key就回导致内存泄漏">所以根源并不是弱引用，而是ThreadLocalMap的生命周期和线程一样长，如果不及时删除对应的key就回导致内存泄漏</h5>
<h2 id="线程协作">线程协作</h2>
<h3 id="等待通知机制">等待通知机制</h3>
<h4 id="等待方">等待方</h4>
<figure data-type="image" tabindex="1"><img src="https://zwangsheng.github.io/post-images/1607568845504.jpg" alt="" loading="lazy"></figure>
<ol>
<li>获得锁</li>
<li>条件不满足就wait，醒了以后还得检查条件</li>
<li>条件满足进入业务</li>
</ol>
<h4 id="通知方">通知方</h4>
<figure data-type="image" tabindex="2"><img src="https://zwangsheng.github.io/post-images/1607568853831.jpg" alt="" loading="lazy"></figure>
<ol>
<li>获得锁</li>
<li>改变条件</li>
<li>通知所有等待的线程</li>
</ol>
<h2 id="线程并发的工具类">线程并发的工具类</h2>
<h3 id="forkjoin">ForkJoin</h3>
<figure data-type="image" tabindex="3"><img src="https://zwangsheng.github.io/post-images/1607569806881.jpg" alt="" loading="lazy"></figure>
<h4 id="recursiveaction用于没有返回结果的任务">RecursiveAction，用于没有返回结果的任务</h4>
<h4 id="recursivetask用于有返回值的任务">RecursiveTask，用于有返回值的任务</h4>
<h4 id="工作密取">工作密取</h4>
<p>线程工作复杂度不同，有的线程先完成任务，那么从别的线程的队列头开始取工作完成<br>
<img src="https://zwangsheng.github.io/post-images/1607569943906.jpg" alt="" loading="lazy"></p>
<h4 id="使用流程">使用流程</h4>
<figure data-type="image" tabindex="4"><img src="https://zwangsheng.github.io/post-images/1607570035989.jpg" alt="" loading="lazy"></figure>
<h3 id="countdownlatch">CountDownLatch</h3>
<p><img src="https://zwangsheng.github.io/post-images/1607570621061.jpg" alt="" loading="lazy"><br>
线程等待其他线程完成工作之后才开始进行<br>
通过一个计数器进行实现，每当一个线程完成任务就进行-1，到达0之后，使用了await()方法的线程就可以往下运行<br>
只能用一次</p>
<h3 id="cyclicbarrier">CyclicBarrier</h3>
<p><img src="https://zwangsheng.github.io/post-images/1607570628459.jpg" alt="" loading="lazy"><br>
等待所有线程到达以后，进行放行<br>
线程达到后调用await()告诉CyclicBarrier已到达，然后将阻塞<br>
可以传入barrierAction，用来所有线程都到达之后先执行这块代码再放行<br>
允许反复使用</p>
<h3 id="semaphore">Semaphore</h3>
<p><img src="https://zwangsheng.github.io/post-images/1607570752314.jpg" alt="" loading="lazy"><br>
一般拿来做流控<br>
acquire() 获取许可证<br>
release() 归还许可证</p>
<h3 id="exchange">Exchange</h3>
<p><img src="https://zwangsheng.github.io/post-images/1607570893818.jpg" alt="" loading="lazy"><br>
用于线程之间的数据交换，提供一个同步点，在这个同步点，允许线程相互交换数据<br>
一个线程调用exchange()，会等待第二个线程也执行exchange()</p>
<h3 id="原子类atomic">原子类Atomic</h3>
<p>利用CAS，volatile进行保证原子性</p>
<h3 id="reentrantlock">ReentrantLock</h3>
<p>显示加锁<br>
可重入，公平非公平靠参数控制，响应式中断<br>
state 保证可重入<br>
fair 控制公平非公平<br>
读写锁的实现通过16位偏移</p>
<h4 id="读写锁的升降级">读写锁的升降级</h4>
<p>在持有写锁的过程中，获得读锁，然后释放写锁</p>
<h3 id="locksupport">LockSupport</h3>
<p>定义了公共的静态方法，提供了基本的线程阻塞和唤醒功能</p>
<h3 id="clh">CLH</h3>
<p>队列锁是一种基于链表的可扩展，高性能，公平的自旋锁<br>
申请的线程仅仅只在本地变量上自旋，不断的轮询前驱节点的状态，前一个节点释放锁了，就结束轮询获得锁</p>
<h4 id="clh流程">CLH流程</h4>
<ol>
<li>创建一个Node，将标识位locked设为true，有一个myPred标识前驱节点</li>
<li>对CLH的tail调用GetAndSet方法，CAS的让自己成为队列的尾部，同时获得前驱引用</li>
<li>轮询前驱节点的locked变成了false，前驱节点被回收，自己成为前驱节点，同时获得锁</li>
</ol>
<h3 id="aqs">AQS</h3>
<p>int 成员变量state作为同步状态，内置的FIFO队列来完成线程的排队，Node存放线程信息</p>
<h2 id="cas">CAS</h2>
<p><img src="https://zwangsheng.github.io/post-images/1607571107049.jpg" alt="" loading="lazy"><br>
获取期望值，在更新之前，判断期望值是不是符合，符合就进行操作</p>
<h3 id="aba问题">ABA问题</h3>
<p>中间发生了变化，但是结果相同，操作不可知<br>
解决办法，加上版本号</p>
<h3 id="循环时间长开销大">循环时间长开销大</h3>
<p>CAS长期自旋，给CPU带来压力</p>
<h3 id="只能保证一个共享变量的原子操作">只能保证一个共享变量的原子操作</h3>
<h2 id="并发容器">并发容器</h2>
<h4 id="17-hashmap扩容死循环">1.7 HashMap扩容死循环</h4>
<p>多个线程进行扩容的时候，一个线程完成了扩容，链表为倒序，另一个把倒序变成了正序，前后相连，形成了环状，当get一个不存在的元素就回无限循环</p>
<h3 id="18-concurrenthashmap">1.8 ConcurrentHashMap</h3>
<p>取消了segments，采用 transient volatile HashEntry&lt;K,V&gt;[] table 进行存储，降低了锁粒度，采用CAS和synchronized来保证安全<br>
将原先 table 数组+单向链表的数据结构，变更为 table 数组+单 向链表+红黑树的结构</p>
<h4 id="concurrenthashmap初始化只需要一个线程">ConcurrentHashMap初始化只需要一个线程</h4>
<h4 id="采取多线程的扩容">采取多线程的扩容</h4>
<h3 id="concurrentskiplist">ConcurrentSkipList</h3>
<p>跳表</p>
<h3 id="concurrentlinkedqueue">ConcurrentLinkedQueue</h3>
<p>LinkedList的并发版本</p>
<h2 id="写时复制容器">写时复制容器</h2>
<p>写入的时候复制一份数据进行写，这时候读到的还是老的数据，保证最终一致<br>
注意复制的内存开销</p>
<h2 id="阻塞队列">阻塞队列</h2>
<p>插入的时候满了阻塞<br>
取的时候空了阻塞</p>
<h3 id="生产者消费者模式">生产者消费者模式</h3>
<p>生产者和消费者不直接通讯，通过阻塞队列进行通讯，阻塞队列相当于缓冲区，平衡了生产者消费者的能力</p>
<h2 id="线程池">线程池</h2>
<ol>
<li>资源消耗降低</li>
<li>提高响应速度</li>
<li>提高线程的可管理性</li>
</ol>
<h3 id="corepoolsize-线程池核心线程数">corePoolSize 线程池核心线程数</h3>
<h3 id="maximumpoolsize-线程池最大线程数">maximumPoolSize 线程池最大线程数</h3>
<h3 id="keepalivetime-线程空闲存活时间对非核心线程才生效">keepAliveTime 线程空闲存活时间，对非核心线程才生效</h3>
<h3 id="workqueue-保存工作的阻塞队列">workQueue 保存工作的阻塞队列</h3>
<h3 id="threadfactory-创建线程的工厂">threadFactory 创建线程的工厂</h3>
<h3 id="rejectedexecutionhandler-拒绝策略阻塞队列满了且达到最大线程数时执行的策略">RejectedExecutionHandler 拒绝策略，阻塞队列满了且达到最大线程数时执行的策略</h3>
<h3 id="线程池工作机制">线程池工作机制</h3>
<ol>
<li>当前线程少于核心数，就造核心线程</li>
<li>当前线程等于核心数，任务塞进阻塞队列</li>
<li>阻塞队列满了，开新的线程处理</li>
<li>线程数达到最大线程数，采取拒绝策略</li>
</ol>
<h3 id="execute-无返回值的提交">execute() 无返回值的提交</h3>
<h3 id="submit有返回值的提交">submit()有返回值的提交</h3>
<h3 id="shutdown-设置线程池状态shutdown中断所有没在执行的线程">shutdown() 设置线程池状态SHUTDOWN，中断所有没在执行的线程</h3>
<h3 id="shutdownnow-设置线程状态stop尝试关闭所有线程返回还在做的列表">shutdownNow() 设置线程状态STOP，尝试关闭所有线程，返回还在做的列表</h3>
<h3 id="isshutdown-并不能代表线程池真的完全关闭">isShutdown() 并不能代表线程池真的完全关闭</h3>
<h3 id="isterminaed-线程池完全关闭">isTerminaed() 线程池完全关闭</h3>
<h4 id="cpu密集型-分配-cpu-1线程的线程池">CPU密集型 分配 CPU + 1线程的线程池</h4>
<h4 id="io密集型-分配cpu-2线程的线程池">IO密集型 分配CPU * 2线程的线程池</h4>
<h4 id="fixedthreadpool-固定线程数的线程池-有界队列">FixedThreadPool 固定线程数的线程池 有界队列</h4>
<h4 id="singlethreadexecutor-单线程线程池-有界队列">SingleThreadExecutor 单线程线程池 有界队列</h4>
<h4 id="cachedthreadpool-缓存线程池无核心线程数无边界队列极端情况下会炸">CachedThreadPool 缓存线程池，无核心线程数，无边界队列，极端情况下会炸</h4>
<h4 id="workstealingpool-工作窃取具体看forkjoin">WorkStealingPool 工作窃取，具体看forkJoin</h4>
<h4 id="scheduledthreadpoolexecutor-定时任务线程池下面还有具体实现">ScheduledThreadPoolExecutor 定时任务线程池，下面还有具体实现</h4>
<h2 id="并发安全">并发安全</h2>
<h3 id="线程封闭">线程封闭</h3>
<p>对象逃不出这个线程</p>
<h3 id="无状态的类">无状态的类</h3>
<h3 id="不可变类">不可变类</h3>
<p>不提供修改的方法</p>
<h3 id="volatile-2">volatile</h3>
<p>保证可见，不保证原子</p>
<h3 id="加锁和-cas">加锁和 CAS</h3>
<h3 id="安全的发布">安全的发布</h3>
<p>发布变量的副本或者线程安全修饰过的对象</p>
<h3 id="theadlocal">TheadLocal</h3>
<h2 id="死锁">死锁</h2>
<p>两个或两个以上的进程在执行过程中，由于竞争资源或者由于彼此通信 而造成的一种阻塞的现象</p>
<h3 id="死锁条件">死锁条件</h3>
<ol>
<li>互斥条件 排他使用</li>
<li>请求和保持 要新的又不放旧的</li>
<li>不剥夺条件 不能强制占有，只能等着主动释放</li>
<li>环路等待条件 形成了环</li>
</ol>
<h3 id="银行家算法">银行家算法</h3>
<h3 id="解决">解决</h3>
<p>通过 jps 查询应用的 id，再通过 jstack id 查看应用的锁的持有情况<br>
采用尝试拿锁的机制</p>
<h2 id="活锁-线程相互谦让拿到锁以后请求另一把锁谦让">活锁 线程相互谦让，拿到锁以后请求另一把锁谦让</h2>
<h2 id="线程饥饿-低优先级的拿不到cpu">线程饥饿 低优先级的拿不到CPU</h2>
<h2 id="并发性能">并发性能</h2>
<h3 id="线程上下文切换的开销">线程上下文切换的开销</h3>
<h3 id="内存同步-加锁等线程安全操作会抑制jvm的优化">内存同步 加锁等线程安全操作会抑制JVM的优化</h3>
<h3 id="长时间阻塞">长时间阻塞</h3>
<h3 id="减少锁的竞争">减少锁的竞争</h3>
<ol>
<li>减小锁粒度</li>
<li>减小锁范围</li>
<li>避免多余的锁</li>
<li>共享锁替代独占锁</li>
</ol>
<h2 id="单例模式">单例模式</h2>
]]></content>
    </entry>
</feed>